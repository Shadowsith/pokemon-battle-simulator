import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.pokemonbattlesim',
  appName: 'Pokemon Battle Simulator',
  webDir: 'www'
};

export default config;
